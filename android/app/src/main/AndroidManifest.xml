<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter's template declares INTERNET only in the debug and profile manifests,
         so a release build would ship with no network access at all. Every screen in
         this app is an HTTPS call to a nestwatch server on the LAN. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Android 13+ requires this to be granted at runtime before any notification is
         shown. Both notification tiers need it; without it the baseline poll runs and
         tells nobody. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- The opt-in "watch now" tier (PLAN §5): a dataSync foreground service the parent
         starts when they are actively waiting for an answer.

         Android 15 (API 35) allows dataSync a total of SIX HOURS per 24, shared across
         every dataSync service the app runs, and only counts time while the app is in
         the background. On exceeding it the system calls Service.onTimeout(int, int) and
         gives a few seconds to stopSelf() before throwing
         RemoteServiceException: "A foreground service of type dataSync did not stop
         within its timeout". targetSdk here is 36, so this applies to us.

         That budget is the whole reason this is the OPT-IN tier and not the baseline:
         a service polling round the clock would be deaf for three quarters of the day.

         Declaring the type ALSO requires a Play Console declaration — updates are
         rejected for omitting it. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Nothing this app stores survives leaving this phone. The pin and the session
         cookie are encrypted under a Keystore key that cannot be exported, so a restored
         copy is unreadable anyway — but a restored install that looks paired and is not
         is a worse first impression than one that plainly asks to pair.

         allowBackup alone is not enough on Android 12+, where cloud backup and
         device-to-device transfer are configured separately. See res/xml/. -->
    <application
        android:label="Nestwatch"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <!-- Play's monitoring-app policy, required from day one.
             Apps are rejected specifically for omitting it, and it must be present in
             EVERY version code across EVERY track — including the first internal-test
             upload, because a track that has ever shipped without it stays blocked.
             See docs/PLAN.md §5, "Store paperwork from day one". -->
        <!-- flutter_foreground_task's service, declared with the dataSync type. The
             plugin ships the class; the type has to be named here, in the app manifest,
             because it is the app that Play judges. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <meta-data
            android:name="isMonitoringTool"
            android:value="child_monitoring" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
